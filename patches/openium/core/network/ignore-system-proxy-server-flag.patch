--- a/chrome/browser/openium_flag_entries.h
+++ b/chrome/browser/openium_flag_entries.h
@@ -70,6 +70,10 @@
      "middleboxes and TLS endpoints mishandle this behavior and may become "
      "inaccessible. Openium flag.",
      kOsAll, FEATURE_VALUE_TYPE(net::features::kOpeniumSplitClientHello)},
+    {"no-proxy-server", "Ignore system proxy",
+     "Ignores system proxy settings and always connects directly. This may "
+     "prevent access to networks that require a proxy. Openium flag.",
+     kOsAll, SINGLE_VALUE_TYPE(switches::kNoProxyServer)},
     // ui
     // platform
 #endif  // CHROME_BROWSER_OPENIUM_FLAG_ENTRIES_H_
