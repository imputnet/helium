--- a/chrome/browser/openium_flag_choices.h
+++ b/chrome/browser/openium_flag_choices.h
@@ -38,6 +38,8 @@ constexpr const char kOpeniumNoiseAudioCo
 constexpr const char kOpeniumNoiseCpuCoresCommandLine[] =
     "openium-noise-hw-concurrency";
 
+constexpr const char kDisableEchCommandLine[] = "disable-ech";
+
 }  // namespace openium
 
 #endif /* CHROME_BROWSER_OPENIUM_FLAG_CHOICES_H_ */
--- a/chrome/browser/openium_flag_entries.h
+++ b/chrome/browser/openium_flag_entries.h
@@ -49,6 +49,11 @@
      "`navigator.hardwareConcurrency` in a reasonable range. Openium flag.",
      kOsAll, FEATURE_VALUE_TYPE(blink::features::kOpeniumNoiseCpuCores)},
     // core/network
+    {openium::kDisableEchCommandLine, "Disable Encrypted Client Hello (ECH)",
+     "Disables TLS Encrypted Client Hello, reducing traffic privacy when "
+     "accessing websites that support ECH. Use this flag only if ECH is "
+     "blocked on your network. Openium flag.",
+     kOsAll, SINGLE_VALUE_TYPE(openium::kDisableEchCommandLine)},
     // ui
     // platform
 #endif  // CHROME_BROWSER_OPENIUM_FLAG_ENTRIES_H_
--- a/chrome/browser/ssl/ssl_config_service_manager.cc
+++ b/chrome/browser/ssl/ssl_config_service_manager.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/command_line.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -253,6 +254,10 @@ network::mojom::SSLConfigPtr SSLConfigSe
 
   config->ech_enabled = ech_enabled_.GetValue();
 
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch("disable-ech")) {
+    config->ech_enabled = false;
+  }
+
 #if BUILDFLAG(CHROME_ROOT_STORE_SUPPORTED)
   config->trust_anchor_ids =
       trust_anchor_ids_.has_value()
