--- a/chrome/browser/openium_flag_choices.h
+++ b/chrome/browser/openium_flag_choices.h
@@ -35,6 +35,8 @@ constexpr const char kMiddleClickAutoscr
 constexpr const char kOpeniumNoiseCommandLine[] = "openium-noise";
 constexpr const char kOpeniumNoiseCanvasCommandLine[] = "openium-noise-canvas";
 constexpr const char kOpeniumNoiseAudioCommandLine[] = "openium-noise-audio";
+constexpr const char kOpeniumNoiseCpuCoresCommandLine[] =
+    "openium-noise-hw-concurrency";
 
 }  // namespace openium
 
--- a/chrome/browser/openium_flag_entries.h
+++ b/chrome/browser/openium_flag_entries.h
@@ -43,6 +43,11 @@
      "Adds insignificant jitter to audio data to deceive AudioContext-based "
      "fingerprinting. Openium flag.",
      kOsAll, FEATURE_VALUE_TYPE(blink::features::kOpeniumNoiseAudio)},
+    {openium::kOpeniumNoiseCpuCoresCommandLine,
+     "[Openium Noise] Randomize number of CPU cores",
+     "Randomizes the number of cores returned by "
+     "`navigator.hardwareConcurrency` in a reasonable range. Openium flag.",
+     kOsAll, FEATURE_VALUE_TYPE(blink::features::kOpeniumNoiseCpuCores)},
     // core/network
     // ui
     // platform
--- a/content/browser/openium_noise/noise_token_data.cc
+++ b/content/browser/openium_noise/noise_token_data.cc
@@ -59,6 +59,8 @@ OpeniumNoiseTokenData::OpeniumNoiseTokenDa
       blink::OpeniumNoiseToken(base::RandUint64());
   session_tokens_[blink::OpeniumNoiseFeature::kAudio] =
       blink::OpeniumNoiseToken(base::RandUint64());
+  session_tokens_[blink::OpeniumNoiseFeature::kHardware] =
+      blink::OpeniumNoiseToken(base::RandUint64());
 }
 
 // static
--- a/third_party/blink/common/features.cc
+++ b/third_party/blink/common/features.cc
@@ -60,6 +60,9 @@ BASE_FEATURE(kOpeniumNoiseCanvas,
 BASE_FEATURE(kOpeniumNoiseAudio,
              base::FEATURE_ENABLED_BY_DEFAULT);
 
+// navigator.hardwareConcurrency noising for fingerprint protection.
+BASE_FEATURE(kOpeniumNoiseCpuCores, base::FEATURE_ENABLED_BY_DEFAULT);
+
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
 BASE_FEATURE(kAIPageContentIncludePopupWindows,
--- a/third_party/blink/public/common/features.h
+++ b/third_party/blink/public/common/features.h
@@ -45,6 +45,7 @@ BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kOpeniumNoise);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kOpeniumNoiseCanvas);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kOpeniumNoiseAudio);
+BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kOpeniumNoiseCpuCores);
 
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
--- a/third_party/blink/public/common/openium_noise/noise_token.h
+++ b/third_party/blink/public/common/openium_noise/noise_token.h
@@ -23,6 +23,7 @@ namespace blink {
 enum class OpeniumNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // A wrapper class for a uint64_t noise token.
--- a/third_party/blink/public/mojom/openium_noise/noise_token.mojom
+++ b/third_party/blink/public/mojom/openium_noise/noise_token.mojom
@@ -8,6 +8,7 @@ module blink.mojom;
 enum OpeniumNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // Typemapped to blink::OpeniumNoiseToken
--- a/third_party/blink/renderer/core/execution_context/navigator_base.cc
+++ b/third_party/blink/renderer/core/execution_context/navigator_base.cc
@@ -9,6 +9,7 @@
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
 #include "third_party/blink/renderer/core/frame/navigator_concurrent_hardware.h"
+#include "third_party/blink/renderer/core/openium_noise/openium_noise_hash.h"
 #include "third_party/blink/renderer/core/probe/core_probes.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
 #include "third_party/blink/renderer/platform/wtf/text/string_builder.h"
@@ -41,6 +42,52 @@ String GetReducedNavigatorPlatform() {
 #endif
 }
 
+constexpr const unsigned kMinNoisedCores = 2;
+constexpr const unsigned kMaxNoisedCores = 16;
+
+std::vector<unsigned> PossibleValuesForCoreCount(unsigned n) {
+  // Clamp the number of cores between [2..=16], as other
+  // configurations are less common.
+  n = std::clamp(n, kMinNoisedCores, kMaxNoisedCores);
+
+  // A machine with a odd number of CPUs is very uncommon.
+  // Floor it to the lower even number.
+  if (n % 2) {
+    n -= 1;
+  }
+
+  // If we can subtract some amount and still end up with
+  // a common CPU count (which is the case for most configurations)
+  // let's add it to the list of possible values.
+  std::vector<unsigned> candidates = {n};
+  if (n > 4) {
+    candidates.push_back(n - 4);
+  }
+
+  if (n > 2) {
+    candidates.push_back(n - 2);
+  }
+
+  return candidates;
+}
+
+unsigned MaybeNoiseCoreCount(ExecutionContext* execution_context, unsigned n) {
+  if (!base::FeatureList::IsEnabled(blink::features::kOpeniumNoiseCpuCores) ||
+      !execution_context ||
+      !execution_context->GetOpeniumNoiseTokens().contains(
+          OpeniumNoiseFeature::kHardware)) {
+    return n;
+  }
+
+  auto token_hash =
+      OpeniumNoiseHash(execution_context->GetOpeniumNoiseTokens().at(
+          OpeniumNoiseFeature::kHardware));
+  token_hash.Update(n);
+
+  auto values = PossibleValuesForCoreCount(n);
+  return values[token_hash.GetValueBelow(values.size())];
+}
+
 }  // namespace
 
 NavigatorBase::NavigatorBase(ExecutionContext* context)
@@ -77,7 +124,7 @@ unsigned int NavigatorBase::hardwareConc
 
   probe::ApplyHardwareConcurrencyOverride(
       probe::ToCoreProbeSink(GetExecutionContext()), hardware_concurrency);
-  return hardware_concurrency;
+  return MaybeNoiseCoreCount(GetExecutionContext(), hardware_concurrency);
 }
 
 ExecutionContext* NavigatorBase::GetUAExecutionContext() const {
--- a/third_party/blink/public/common/openium_noise/noise_token_mojom_traits.h
+++ b/third_party/blink/public/common/openium_noise/noise_token_mojom_traits.h
@@ -30,6 +30,8 @@ struct BLINK_COMMON_EXPORT
         return blink::mojom::OpeniumNoiseFeature::kCanvas;
       case blink::OpeniumNoiseFeature::kAudio:
         return blink::mojom::OpeniumNoiseFeature::kAudio;
+      case blink::OpeniumNoiseFeature::kHardware:
+        return blink::mojom::OpeniumNoiseFeature::kHardware;
     }
     NOTREACHED();
   }
@@ -41,6 +43,8 @@ struct BLINK_COMMON_EXPORT
         return blink::OpeniumNoiseFeature::kCanvas;
       case blink::mojom::OpeniumNoiseFeature::kAudio:
         return blink::OpeniumNoiseFeature::kAudio;
+      case blink::mojom::OpeniumNoiseFeature::kHardware:
+        return blink::OpeniumNoiseFeature::kHardware;
     }
 
     NOTREACHED();
