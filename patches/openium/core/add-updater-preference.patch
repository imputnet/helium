--- a/components/openium_services/pref_names.h
+++ b/components/openium_services/pref_names.h
@@ -33,6 +33,9 @@ inline constexpr char kOpeniumExtProxyEna
 inline constexpr char kOpeniumBangsEnabled[] =
     "openium.services.bangs";
 
+inline constexpr char kOpeniumUpdateFetchingEnabled[] =
+    "openium.services.browser_updates";
+
 inline constexpr char kOpeniumSpellcheckEnabled[] =
     "openium.services.spellcheck_files";
 
--- a/components/openium_services/openium_services_helpers.h
+++ b/components/openium_services/openium_services_helpers.h
@@ -22,11 +22,13 @@ bool ShouldAccessServices(const PrefServ
 
 COMPONENT_EXPORT(OPENIUM) bool ShouldFetchBangs(const PrefService& prefs);
 COMPONENT_EXPORT(OPENIUM) bool ShouldAccessExtensionService(const PrefService& prefs);
+COMPONENT_EXPORT(OPENIUM) bool ShouldAccessUpdateService(const PrefService& prefs);
 COMPONENT_EXPORT(OPENIUM) GURL GetServicesBaseURL(const PrefService& prefs);
 COMPONENT_EXPORT(OPENIUM) GURL GetDummyURL();
 COMPONENT_EXPORT(OPENIUM) GURL GetExtensionUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(OPENIUM) GURL GetWebstoreSnippetURL(const PrefService& prefs, std::string_view id);
 COMPONENT_EXPORT(OPENIUM) GURL GetSpellcheckURL(const PrefService& prefs);
+COMPONENT_EXPORT(OPENIUM) GURL GetBrowserUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(OPENIUM) std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_);
 COMPONENT_EXPORT(OPENIUM) void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
                             PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer);
--- a/components/openium_services/openium_services_helpers.cc
+++ b/components/openium_services/openium_services_helpers.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/command_line.h"
 #include "base/functional/bind.h"
 #include "base/strings/stringprintf.h"
 #include "components/openium_services/pref_names.h"
@@ -15,6 +16,28 @@
 #include "url/url_constants.h"
 
 namespace openium {
+#if defined(ARCH_CPU_ARM64)
+#define CPU_ARCH_STR "arm64"
+#elif defined(ARCH_CPU_X86_64)
+#define CPU_ARCH_STR "x86_64"
+#endif
+
+#if defined(CPU_ARCH_STR) && (BUILDFLAG(IS_MAC) || BUILDFLAG(IS_WIN))
+#define UPDATES_SUPPORTED
+#endif
+
+#if defined(UPDATES_SUPPORTED)
+constexpr std::string_view kUpdateBaseUrl = "";
+constexpr const char kUpdatePath[] =
+#if BUILDFLAG(IS_MAC)
+    "mac/appcast-" CPU_ARCH_STR ".xml";
+#elif BUILDFLAG(IS_WIN)
+    "win/appcast.xml";
+#endif
+
+// Duplicated from openium_flag_choices.h to avoid importing it.
+constexpr const char kUpdateServerUrlCommandLine[] = "custom-update-server-url";
+#endif
 
 std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_) {
     if (user_url_.empty()) {
@@ -73,6 +96,11 @@ bool ShouldFetchBangs(const PrefService&
             prefs.GetBoolean(prefs::kOpeniumBangsEnabled);
 }
 
+bool ShouldAccessUpdateService(const PrefService& prefs) {
+  return ShouldAccessServices(prefs) &&
+         prefs.GetBoolean(prefs::kOpeniumUpdateFetchingEnabled);
+}
+
 GURL GetExtensionUpdateURL(const PrefService& prefs) {
     if (!ShouldAccessExtensionService(prefs)) {
         return GetDummyURL();
@@ -98,6 +126,24 @@ GURL GetSpellcheckURL(const PrefService&
     return GetServicesBaseURL(prefs).Resolve("/dict/");
 }
 
+GURL GetBrowserUpdateURL(const PrefService& prefs) {
+  if (!ShouldAccessUpdateService(prefs)) {
+    return GetDummyURL();
+  }
+
+#if defined(UPDATES_SUPPORTED)
+  const auto* command_line = base::CommandLine::ForCurrentProcess();
+  std::string baseUrl(kUpdateBaseUrl);
+  if (command_line->HasSwitch(openium::kUpdateServerUrlCommandLine)) {
+    baseUrl =
+        command_line->GetSwitchValueASCII(openium::kUpdateServerUrlCommandLine);
+  }
+  return GURL(baseUrl).Resolve(kUpdatePath);
+#else
+  return GetDummyURL();
+#endif
+}
+
 void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
     PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer) {
   registrar.Add(prefs::kOpeniumServicesEnabled, observer);
--- a/chrome/browser/resources/settings/privacy_page/services_page.html
+++ b/chrome/browser/resources/settings/privacy_page/services_page.html
@@ -99,6 +99,11 @@
                     sub-label="$i18n{openiumSpellcheckToggleDescription}">
                 </settings-toggle-button>
             </if>
+            <settings-toggle-button id="updatesToggleButton"
+                pref="{{prefs.openium.services.browser_updates}}"
+                label="$i18n{openiumUpdatesToggle}"
+                sub-label="$i18n{openiumUpdatesToggleDescription}">
+            </settings-toggle-button>
         </cr-collapse>
     </div>
     </template>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -2497,6 +2497,9 @@ void AddPrivacyStrings(content::WebUIDat
       {"openiumSpellcheckToggle", IDS_SETTINGS_OPENIUM_SERVICES_SPELLCHECK_TOGGLE},
       {"openiumSpellcheckToggleDescription",
         IDS_SETTINGS_OPENIUM_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION},
+      {"openiumUpdatesToggle", IDS_SETTINGS_OPENIUM_SERVICES_UPDATE},
+      {"openiumUpdatesToggleDescription",
+        IDS_SETTINGS_OPENIUM_SERVICES_UPDATE_DESCRIPTION},
       {"openiumOriginOverride", IDS_SETTINGS_OPENIUM_SERVICES_OVERRIDE},
       {"openiumOriginOverrideDescription",
         IDS_SETTINGS_OPENIUM_SERVICES_OVERRIDE_DESCRIPTION},
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -446,6 +446,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kOpeniumSpellcheckEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kOpeniumUpdateFetchingEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kOpeniumServicesOrigin] =
       settings_api::PrefType::kString;
   (*s_allowlist)[::prefs::kOpeniumServicesConsented] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -230,6 +230,7 @@ void RegisterBrowserUserPrefs(user_prefs
     registry->RegisterStringPref(prefs::kOpeniumServicesOrigin, "");
     registry->RegisterBooleanPref(prefs::kOpeniumDidOnboarding, false);
     registry->RegisterBooleanPref(prefs::kOpeniumServicesConsented, false);
+    registry->RegisterBooleanPref(prefs::kOpeniumUpdateFetchingEnabled, false);
   }
 
   registry->RegisterBooleanPref(
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -1764,6 +1764,22 @@
     <message name="IDS_SETTINGS_OPENIUM_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION" desc="Description of the for enabling/disabling of downloading spellcheck files">
       Openium will fetch dictionary files used for spell checking when requested. When disabled, spell checking will not work.
     </message>
+    <if expr="not is_linux">
+      <message name="IDS_SETTINGS_OPENIUM_SERVICES_UPDATE" desc="Toggle for automatic update downloads">
+        Allow automatic browser updates
+      </message>
+      <message name="IDS_SETTINGS_OPENIUM_SERVICES_UPDATE_DESCRIPTION" desc="Description of the toggle for automatic update downloads">
+        Openium will automatically check for updates and install them. This includes browser and component updates. We recommend keeping this setting enabled to ensure you get the latest features and security updates.
+      </message>
+    </if>
+    <if expr="is_linux">
+      <message name="IDS_SETTINGS_OPENIUM_SERVICES_UPDATE" desc="Toggle for automatic update downloads">
+        Allow automatic component updates
+      </message>
+      <message name="IDS_SETTINGS_OPENIUM_SERVICES_UPDATE_DESCRIPTION" desc="Description of the toggle for automatic update downloads">
+        Openium will automatically check for component updates and install them. We recommend keeping this setting enabled. On Linux, browser updates are handled by your distro's package manager. Please check for Openium package updates at least every week.
+      </message>
+    </if>
     <message name="IDS_SETTINGS_OPENIUM_SERVICES_OVERRIDE" desc="Text input for overriding the Openium services server">
       Use your own instance of Openium services
     </message>
--- a/chrome/browser/openium_flag_choices.h
+++ b/chrome/browser/openium_flag_choices.h
@@ -27,6 +27,8 @@ constexpr const FeatureEntry::Choice kCh
     {kChannelBeta, kChannelCommandLine, kChannelBeta},
 };
 
+constexpr const char kUpdateServerUrlCommandLine[] = "custom-update-server-url";
+
 }  // namespace openium
 
 #endif /* CHROME_BROWSER_OPENIUM_FLAG_CHOICES_H_ */
--- a/chrome/browser/openium_flag_entries.h
+++ b/chrome/browser/openium_flag_entries.h
@@ -19,6 +19,10 @@
     {openium::kChannelCommandLine, "Update channel",
      "Selects which update channel to use for update checking. Openium flag.",
      kOsAll, MULTI_VALUE_TYPE(openium::kChannelChoices)},
+    {openium::kUpdateServerUrlCommandLine, "Update server URL",
+     "Overrides the default URL for fetching updates. Should be used only for "
+     "testing. Openium flag.",
+     kOsDesktop, STRING_VALUE_TYPE(openium::kUpdateServerUrlCommandLine, "")},
     // core/network
     // ui
     // platform
