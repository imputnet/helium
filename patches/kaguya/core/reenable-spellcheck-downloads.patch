--- a/chrome/browser/spellchecker/spellcheck_hunspell_dictionary.cc
+++ b/chrome/browser/spellchecker/spellcheck_hunspell_dictionary.cc
@@ -24,8 +24,10 @@
 #include "base/task/thread_pool.h"
 #include "base/threading/scoped_blocking_call.h"
 #include "build/build_config.h"
+#include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/spellchecker/spellcheck_service.h"
 #include "chrome/common/chrome_paths.h"
+#include "components/kaguya_services/kaguya_services_helpers.h"
 #include "components/spellcheck/browser/spellcheck_platform.h"
 #include "components/spellcheck/common/spellcheck_common.h"
 #include "components/spellcheck/common/spellcheck_features.h"
@@ -277,15 +279,26 @@ GURL SpellcheckHunspellDictionary::GetDi
   std::string bdict_file = dictionary_file_.path.BaseName().MaybeAsASCII();
   DCHECK(!bdict_file.empty());
 
-  static const char kDownloadServerUrl[] =
-      "https://redirector.gvt1.com/edgedl/chrome/dict/";
+  if (browser_context_) {
+    const Profile* profile = Profile::FromBrowserContext(browser_context_);
+    if (!profile) {
+      goto fail;
+    }
+
+    const PrefService* prefs = profile->GetPrefs();
+    if (!prefs) {
+      goto fail;
+    }
+
+    return kaguya::GetSpellcheckURL(*prefs)
+            .Resolve(bdict_file);
+  }
 
-  return GURL(std::string(kDownloadServerUrl) +
-              base::ToLowerASCII(bdict_file));
+fail:
+  return GURL("https://failed-getting-spellcheck.qjz9zk/");
 }
 
 void SpellcheckHunspellDictionary::DownloadDictionary(GURL url) {
-  return;
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   DCHECK(browser_context_);
 
--- a/components/kaguya_services/kaguya_services_helpers.cc
+++ b/components/kaguya_services/kaguya_services_helpers.cc
@@ -63,6 +63,11 @@ bool ShouldAccessExtensionService(const
             prefs.GetBoolean(prefs::kKaguyaExtProxyEnabled);
 }
 
+bool ShouldAccessDictionaryService(const PrefService& prefs) {
+    return ShouldAccessServices(prefs) &&
+            prefs.GetBoolean(prefs::kKaguyaSpellcheckEnabled);
+}
+
 bool ShouldFetchBangs(const PrefService& prefs) {
     return ShouldAccessServices(prefs) &&
             prefs.GetBoolean(prefs::kKaguyaBangsEnabled);
@@ -85,6 +90,14 @@ GURL GetWebstoreSnippetURL(const PrefSer
         base::StringPrintf("/ext/cws_snippet?id=%s", id));
 }
 
+GURL GetSpellcheckURL(const PrefService& prefs) {
+    if (!ShouldAccessDictionaryService(prefs)) {
+        return GetDummyURL();
+    }
+
+    return GetServicesBaseURL(prefs).Resolve("/dict/");
+}
+
 void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
     PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer) {
   registrar.Add(prefs::kKaguyaServicesEnabled, observer);
--- a/components/kaguya_services/kaguya_services_helpers.h
+++ b/components/kaguya_services/kaguya_services_helpers.h
@@ -26,6 +26,7 @@ COMPONENT_EXPORT(KAGUYA) GURL GetService
 COMPONENT_EXPORT(KAGUYA) GURL GetDummyURL();
 COMPONENT_EXPORT(KAGUYA) GURL GetExtensionUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) GURL GetWebstoreSnippetURL(const PrefService& prefs, std::string_view id);
+COMPONENT_EXPORT(KAGUYA) GURL GetSpellcheckURL(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_);
 COMPONENT_EXPORT(KAGUYA) void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
                             PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer);
--- a/components/kaguya_services/pref_names.h
+++ b/components/kaguya_services/pref_names.h
@@ -33,6 +33,9 @@ inline constexpr char kKaguyaExtProxyEna
 inline constexpr char kKaguyaBangsEnabled[] =
     "kaguya.services.bangs";
 
+inline constexpr char kKaguyaSpellcheckEnabled[] =
+    "kaguya.services.spellcheck_files";
+
 }  // namespace prefs
 
 #endif  // COMPONENTS_KAGUYA_SERVICES_PREF_NAMES_H_
--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -8422,6 +8422,8 @@ static_library("test_support") {
     deps += [
       "//chrome/browser/spellchecker",
       "//components/spellcheck/common",
+      "//components/kaguya_services",
+      "//chrome/browser/profiles:profile",
     ]
   }
   if (enable_bound_session_credentials) {
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -401,6 +401,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kKaguyaBangsEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kKaguyaSpellcheckEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kKaguyaServicesOrigin] =
       settings_api::PrefType::kString;
   (*s_allowlist)[::prefs::kKaguyaServicesConsented] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -225,6 +225,7 @@ void RegisterBrowserUserPrefs(user_prefs
     registry->RegisterBooleanPref(prefs::kKaguyaServicesEnabled, true);
     registry->RegisterBooleanPref(prefs::kKaguyaBangsEnabled, true);
     registry->RegisterBooleanPref(prefs::kKaguyaExtProxyEnabled, true);
+    registry->RegisterBooleanPref(prefs::kKaguyaSpellcheckEnabled, true);
     registry->RegisterStringPref(prefs::kKaguyaServicesOrigin, "");
     registry->RegisterBooleanPref(prefs::kKaguyaDidOnboarding, false);
     registry->RegisterBooleanPref(prefs::kKaguyaServicesConsented, false);
--- a/chrome/browser/resources/settings/privacy_page/services_page.html
+++ b/chrome/browser/resources/settings/privacy_page/services_page.html
@@ -92,6 +92,13 @@
                 label="$i18n{kaguyaBangsToggle}"
                 sub-label="$i18n{kaguyaBangsToggleDescription}">
             </settings-toggle-button>
+            <if expr="use_renderer_spellchecker">
+                <settings-toggle-button id="spellcheckToggleButton"
+                    pref="{{prefs.kaguya.services.spellcheck_files}}"
+                    label="$i18n{kaguyaSpellcheckToggle}"
+                    sub-label="$i18n{kaguyaSpellcheckToggleDescription}">
+                </settings-toggle-button>
+            </if>
         </cr-collapse>
     </div>
     </template>
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -1300,6 +1300,9 @@
       <message name="IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP" desc="Error message when spell dictionary download fails more than once possibly due to a network policy or configuration.">
         Please check with your network administrator to make sure that the firewall is not blocking downloads from Google servers.
       </message>
+      <message name="IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP_KAGUYA" desc="Error message when spell dictionary download fails more than once possibly due to a network policy or configuration.">
+        Please check that Kaguya services are enabled (including spell check downloads) and make sure that the firewall is not blocking downloads from Kaguya servers.
+      </message>
     </if>
   </if>
   <if expr="is_chromeos">
@@ -2005,6 +2008,12 @@
   <message name="IDS_SETTINGS_KAGUYA_SERVICES_BANGS_TOGGLE_DESCRIPTION" desc="Description of the toggle for enabling/disabling of downloading bangs">
     Kaguya will fetch a list of bangs that help you browse the Internet faster, such as !w or !gh. When disabled, bangs will not work.
   </message>
+  <message name="IDS_SETTINGS_KAGUYA_SERVICES_SPELLCHECK_TOGGLE" desc="Toggle for enabling/disabling of downloading spellcheck files">
+    Allow downloading dictionary files for spell checking
+  </message>
+  <message name="IDS_SETTINGS_KAGUYA_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION" desc="Description of the for enabling/disabling of downloading spellcheck files">
+    Kaguya will fetch dictionary files used for spell checking when requested. When disabled, spell checking will not work.
+  </message>
   <message name="IDS_SETTINGS_KAGUYA_SERVICES_OVERRIDE" desc="Text input for overriding the Kaguya services server">
     Use your own instance of Kaguya services
   </message>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -1292,7 +1292,7 @@ void AddLanguagesStrings(content::WebUID
       {"languagesDictionaryDownloadError",
        IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED},
       {"languagesDictionaryDownloadErrorHelp",
-       IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP},
+       IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP_KAGUYA},
 #endif
 #endif  // !BUILDFLAG(IS_CHROMEOS)
 #if BUILDFLAG(IS_CHROMEOS)
@@ -2250,6 +2250,9 @@ void AddPrivacyStrings(content::WebUIDat
       {"kaguyaBangsToggle", IDS_SETTINGS_KAGUYA_SERVICES_BANGS_TOGGLE},
       {"kaguyaBangsToggleDescription",
         IDS_SETTINGS_KAGUYA_SERVICES_BANGS_TOGGLE_DESCRIPTION},
+      {"kaguyaSpellcheckToggle", IDS_SETTINGS_KAGUYA_SERVICES_SPELLCHECK_TOGGLE},
+      {"kaguyaSpellcheckToggleDescription",
+        IDS_SETTINGS_KAGUYA_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION},
       {"kaguyaOriginOverride", IDS_SETTINGS_KAGUYA_SERVICES_OVERRIDE},
       {"kaguyaOriginOverrideDescription",
         IDS_SETTINGS_KAGUYA_SERVICES_OVERRIDE_DESCRIPTION},
--- a/chrome/common/features.gni
+++ b/chrome/common/features.gni
@@ -10,6 +10,7 @@ import("//components/compose/features.gn
 import("//components/safe_browsing/buildflags.gni")
 import("//components/sharing_message/buildflags.gni")
 import("//components/signin/features.gni")
+import("//components/spellcheck/spellcheck_build_features.gni")
 import("//components/supervised_user/buildflags.gni")
 import("//content/public/common/features.gni")
 import("//crypto/features.gni")
@@ -109,4 +110,5 @@ chrome_grit_defines = [
   "optimize_webui=$optimize_webui",
   "chrome_root_store_cert_management_ui=$chrome_root_store_cert_management_ui",
   "use_nss_certs=$use_nss_certs",
+  "use_renderer_spellchecker=$use_renderer_spellchecker"
 ]
