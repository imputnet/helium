Index: src/components/kaguya_services/kaguya_services_helpers.cc
===================================================================
--- src.orig/components/kaguya_services/kaguya_services_helpers.cc	2026-06-20 10:17:05.345758011 +0800
+++ src/components/kaguya_services/kaguya_services_helpers.cc	2026-06-20 10:18:12.796964626 +0800
@@ -85,6 +85,11 @@
             prefs.GetBoolean(prefs::kKaguyaUpdateFetchingEnabled);
 }
 
+bool ShouldAccessUBlockAssets(const PrefService& prefs) {
+    return ShouldAccessServices(prefs) &&
+            prefs.GetBoolean(prefs::kKaguyaUBlockAssetsEnabled);
+}
+
 GURL GetExtensionUpdateURL(const PrefService& prefs) {
     if (!ShouldAccessExtensionService(prefs)) {
         return GetDummyURL();
@@ -124,6 +129,14 @@
 #endif
 }
 
+GURL GetUBlockAssetsURL(const PrefService& prefs) {
+    if (!ShouldAccessUBlockAssets(prefs)) {
+        return GetDummyURL();
+    }
+
+    return GetServicesBaseURL(prefs).Resolve("/ubo/assets.json");
+}
+
 void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
     PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer) {
   registrar.Add(prefs::kKaguyaServicesEnabled, observer);
Index: src/components/kaguya_services/kaguya_services_helpers.h
===================================================================
--- src.orig/components/kaguya_services/kaguya_services_helpers.h	2026-06-20 10:17:05.345672640 +0800
+++ src/components/kaguya_services/kaguya_services_helpers.h	2026-06-20 10:18:12.797135659 +0800
@@ -23,12 +23,14 @@
 COMPONENT_EXPORT(KAGUYA) bool ShouldFetchBangs(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) bool ShouldAccessExtensionService(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) bool ShouldAccessUpdateService(const PrefService& prefs);
+COMPONENT_EXPORT(KAGUYA) bool ShouldAccessUBlockAssets(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) GURL GetServicesBaseURL(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) GURL GetDummyURL();
 COMPONENT_EXPORT(KAGUYA) GURL GetExtensionUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) GURL GetWebstoreSnippetURL(const PrefService& prefs, std::string_view id);
 COMPONENT_EXPORT(KAGUYA) GURL GetSpellcheckURL(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) GURL GetBrowserUpdateURL(const PrefService& prefs);
+COMPONENT_EXPORT(KAGUYA) GURL GetUBlockAssetsURL(const PrefService& prefs);
 COMPONENT_EXPORT(KAGUYA) std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_);
 COMPONENT_EXPORT(KAGUYA) void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
                             PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer);
Index: src/components/kaguya_services/pref_names.h
===================================================================
--- src.orig/components/kaguya_services/pref_names.h	2026-06-20 10:17:05.345500348 +0800
+++ src/components/kaguya_services/pref_names.h	2026-06-20 10:18:12.797202780 +0800
@@ -31,6 +31,8 @@
 inline constexpr char kKaguyaSpellcheckEnabled[] =
     "kaguya.services.spellcheck_files";
 
+inline constexpr char kKaguyaUBlockAssetsEnabled[] =
+    "kaguya.services.ublock_assets";
 }  // namespace prefs
 
 #endif  // COMPONENTS_KAGUYA_SERVICES_PREF_NAMES_H_
Index: src/chrome/browser/extensions/api/settings_private/prefs_util.cc
===================================================================
--- src.orig/chrome/browser/extensions/api/settings_private/prefs_util.cc	2026-06-20 10:17:05.346781441 +0800
+++ src/chrome/browser/extensions/api/settings_private/prefs_util.cc	2026-06-20 10:18:12.797377673 +0800
@@ -405,6 +405,8 @@
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kKaguyaUpdateFetchingEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kKaguyaUBlockAssetsEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kKaguyaServicesOrigin] =
       settings_api::PrefType::kString;
 
Index: src/chrome/browser/ui/browser_ui_prefs.cc
===================================================================
--- src.orig/chrome/browser/ui/browser_ui_prefs.cc	2026-06-20 10:17:05.347035534 +0800
+++ src/chrome/browser/ui/browser_ui_prefs.cc	2026-06-20 10:18:12.797669267 +0800
@@ -227,6 +227,7 @@
     registry->RegisterBooleanPref(prefs::kKaguyaExtProxyEnabled, true);
     registry->RegisterBooleanPref(prefs::kKaguyaSpellcheckEnabled, true);
     registry->RegisterBooleanPref(prefs::kKaguyaUpdateFetchingEnabled, true);
+    registry->RegisterBooleanPref(prefs::kKaguyaUBlockAssetsEnabled, true);
     registry->RegisterStringPref(prefs::kKaguyaServicesOrigin, "");
   }
 
Index: src/chrome/browser/extensions/extension_service.cc
===================================================================
--- src.orig/chrome/browser/extensions/extension_service.cc	2026-06-20 10:12:51.893066788 +0800
+++ src/chrome/browser/extensions/extension_service.cc	2026-06-20 10:18:12.797827670 +0800
@@ -31,6 +31,7 @@
 #include "base/strings/utf_string_conversions.h"
 #include "base/syslog_logging.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/task/thread_pool.h"
 #include "base/threading/thread_restrictions.h"
 #include "base/time/time.h"
 #include "base/trace_event/trace_event.h"
@@ -70,6 +71,9 @@
 #include "chrome/common/pref_names.h"
 #include "chrome/common/url_constants.h"
 #include "components/crx_file/id_util.h"
+#include "components/kaguya_services/extension_ids.h"
+#include "components/kaguya_services/kaguya_services_helpers.h"
+#include "components/kaguya_services/pref_names.h"
 #include "components/policy/core/common/policy_pref_names.h"
 #include "components/supervised_user/core/browser/supervised_user_preferences.h"
 #include "content/public/browser/browser_thread.h"
@@ -208,6 +212,7 @@
       ready_(ready),
       updater_(ExtensionUpdater::Get(profile)),
       component_loader_(ComponentLoader::Get(profile_)),
+      can_access_ublock_assets_(kaguya::ShouldAccessUBlockAssets(*profile_->GetPrefs())),
       error_controller_(error_controller),
       external_install_manager_(ExternalInstallManager::Get(profile)),
       extension_registrar_delegate_(
@@ -289,6 +294,33 @@
       prefs::kExtensionsUIDeveloperMode,
       base::BindRepeating(&ExtensionService::OnDeveloperModePrefChanged,
                           base::Unretained(this)));
+  kaguya::ConfigurePrefChangeRegistrarFor(
+    prefs::kKaguyaUBlockAssetsEnabled,
+    pref_change_registrar_,
+    base::BindRepeating(&ExtensionService::ToggleUBlockExtension,
+                        base::Unretained(this)));
+}
+
+void ExtensionService::ToggleUBlockExtension() {
+  if (!extension_registrar_) {
+    return;
+  }
+
+  bool has_permission_now = kaguya::ShouldAccessUBlockAssets(*profile_->GetPrefs());
+  if (can_access_ublock_assets_ == has_permission_now) {
+    return;
+  }
+
+  can_access_ublock_assets_ = has_permission_now;
+
+  for (int seconds : {0, 5}) {
+    content::GetUIThreadTaskRunner({base::TaskPriority::BEST_EFFORT})->PostDelayedTask(
+        FROM_HERE,
+        base::BindOnce(&ExtensionRegistrar::ReloadExtension,
+                        extension_registrar_->GetWeakPtr(),
+                        kaguya::kUBlockOriginComponentId),
+        base::Seconds(seconds));
+  }
 }
 
 base::WeakPtr<ExtensionServiceInterface> ExtensionService::AsWeakPtr() {
Index: src/chrome/browser/extensions/extension_service.h
===================================================================
--- src.orig/chrome/browser/extensions/extension_service.h	2026-06-20 10:12:51.102762258 +0800
+++ src/chrome/browser/extensions/extension_service.h	2026-06-20 10:18:12.797971012 +0800
@@ -169,6 +169,8 @@
   // KeyedService two-phase shutdown.
   void Shutdown();
 
+  void ToggleUBlockExtension();
+
   // Performs action based on verdicts received from the Extension Telemetry
   // server. Currently, these verdicts are limited to off-store extensions.
   void PerformActionBasedOnExtensionTelemetryServiceVerdicts(
@@ -359,6 +361,8 @@
   // Used in test to ensure the service's shutdown method has been called.
   bool is_shut_down_executed_ = false;
 
+  bool can_access_ublock_assets_;
+
   // The controller for the UI that alerts the user about any blocklisted
   // extensions. Not owned.
   raw_ptr<ExtensionErrorController> error_controller_ = nullptr;
Index: src/extensions/browser/api/storage/BUILD.gn
===================================================================
--- src.orig/extensions/browser/api/storage/BUILD.gn	2026-05-19 07:12:01.000000000 +0800
+++ src/extensions/browser/api/storage/BUILD.gn	2026-06-20 10:18:12.798081374 +0800
@@ -60,6 +60,7 @@
     ":settings_namespace",
     ":settings_observer",
     "//base",
+    "//components/kaguya_services",
     "//components/keyed_service/content",
     "//components/value_store",
     "//content/public/browser",
Index: src/extensions/browser/api/storage/storage_frontend.cc
===================================================================
--- src.orig/extensions/browser/api/storage/storage_frontend.cc	2026-05-19 07:12:01.000000000 +0800
+++ src/extensions/browser/api/storage/storage_frontend.cc	2026-06-20 10:18:12.798261786 +0800
@@ -20,6 +20,8 @@
 #include "base/strings/string_util.h"
 #include "base/strings/stringprintf.h"
 #include "base/trace_event/trace_event.h"
+#include "chrome/browser/profiles/profile.h"
+#include "components/kaguya_services/kaguya_services_helpers.h"
 #include "components/value_store/value_store_factory.h"
 #include "content/public/browser/browser_context.h"
 #include "content/public/browser/browser_thread.h"
@@ -276,6 +278,24 @@
 
   if (success) {
     get_result.data = result.PassSettings();
+
+    Profile* profile_ = Profile::FromBrowserContext(browser_context_);
+
+    if (Manifest::IsUBlockComponent(extension_id)
+        && storage_area == StorageAreaNamespace::kManaged
+        && kaguya::ShouldAccessUBlockAssets(*profile_->GetPrefs())) {
+
+      if (!get_result.data->FindDict("adminSettings")) {
+        get_result.data->Set("adminSettings", base::DictValue());
+      }
+
+      base::DictValue* adminSettings = get_result.data->FindDict("adminSettings");
+
+      if (!adminSettings->Find("assetsBootstrapLocation")) {
+        adminSettings->Set("assetsBootstrapLocation",
+          base::Value(kaguya::GetUBlockAssetsURL(*profile_->GetPrefs()).spec()));
+      }
+    }
   }
 
   std::move(callback).Run(std::move(get_result));
Index: src/third_party/ublock/js/assets.js
===================================================================
--- src.orig/third_party/ublock/js/assets.js	2026-06-20 10:11:39.657584819 +0800
+++ src/third_party/ublock/js/assets.js	2026-06-20 10:18:12.798417009 +0800
@@ -525,7 +525,7 @@
 function getAssetSourceRegistry() {
     if ( assetSourceRegistryPromise === undefined ) {
         assetSourceRegistryPromise = cacheStorage.get(
-            'assetSourceRegistry'
+            'assetSourceRegistry' + (µb.assetsBootstrapLocation ?? '')
         ).then(bin => {
             if ( bin instanceof Object ) {
                 if ( bin.assetSourceRegistry instanceof Object ) {
Index: src/chrome/app/settings_strings.grdp
===================================================================
--- src.orig/chrome/app/settings_strings.grdp	2026-06-20 10:17:05.347466738 +0800
+++ src/chrome/app/settings_strings.grdp	2026-06-20 10:18:12.798954567 +0800
@@ -2021,6 +2021,12 @@
       Kaguya will automatically download and install browser and component updates as they become available. We recommend keeping this setting enabled to ensure you get the latest features and security updates. Automatic core browser updates are not available on this platform yet, but component updates are. Please use external software to keep Kaguya up to date.
     </message>
   </if>
+  <message name="IDS_SETTINGS_KAGUYA_SERVICES_UBO" desc="Description of the toggle for uBO filter lists">
+    Allow downloading filter lists for uBlock Origin
+  </message>
+  <message name="IDS_SETTINGS_KAGUYA_SERVICES_UBO_DESCRIPTION" desc="Description of the toggle for automatic update downloads">
+    Kaguya will fetch fresh filter lists for uBlock Origin. All requests to lists are proxied to protect your privacy. When disabled, default filter lists will be loaded from local storage, which are only updated along with Kaguya. Optional filter lists will be requested without proxying.
+  </message>
   <message name="IDS_SETTINGS_KAGUYA_SERVICES_OVERRIDE" desc="Text input for overriding the Kaguya services server">
     Use your own instance of Kaguya services
   </message>
Index: src/chrome/browser/resources/settings/privacy_page/services_page.html
===================================================================
--- src.orig/chrome/browser/resources/settings/privacy_page/services_page.html	2026-06-20 10:17:05.345851542 +0800
+++ src/chrome/browser/resources/settings/privacy_page/services_page.html	2026-06-20 10:18:12.799546376 +0800
@@ -77,6 +77,11 @@
                 label="$i18n{kaguyaUpdatesToggle}"
                 sub-label="$i18n{kaguyaUpdatesToggleDescription}">
             </settings-toggle-button>
+            <settings-toggle-button id="filterlistToggleButton"
+                pref="{{prefs.kaguya.services.ublock_assets}}"
+                label="$i18n{kaguyaUboToggle}"
+                sub-label="$i18n{kaguyaUboToggleDescription}">
+            </settings-toggle-button>
         </cr-collapse>
     </div>
 </div>
Index: src/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
===================================================================
--- src.orig/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc	2026-06-20 10:17:05.346262056 +0800
+++ src/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc	2026-06-20 10:18:12.799874601 +0800
@@ -2255,6 +2255,10 @@
         IDS_SETTINGS_KAGUYA_SERVICES_UPDATE},
       {"kaguyaUpdatesToggleDescription",
         IDS_SETTINGS_KAGUYA_SERVICES_UPDATE_DESCRIPTION},
+      {"kaguyaUboToggle",
+        IDS_SETTINGS_KAGUYA_SERVICES_UBO},
+      {"kaguyaUboToggleDescription",
+        IDS_SETTINGS_KAGUYA_SERVICES_UBO_DESCRIPTION},
       {"kaguyaOriginOverride", IDS_SETTINGS_KAGUYA_SERVICES_OVERRIDE},
       {"kaguyaOriginOverrideDescription",
         IDS_SETTINGS_KAGUYA_SERVICES_OVERRIDE_DESCRIPTION},
