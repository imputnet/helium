Reverts https://crrev.com/c/7683270
TODO: Remove this patch once depot_tools is rolled in DEPS

--- a/build/config/compiler/BUILD.gn
+++ b/build/config/compiler/BUILD.gn
@@ -3433,15 +3433,6 @@ if (is_android) {
         "-Wl,--no-warn-symbol-ordering",
       ]
       inputs = [ chrome_orderfile_path ]
-
-      # These are txt files used by gclient to decide which version to download.
-      # They have zero effect on the build. We add them here merely so that
-      # GN-analyze recognizes them as dependencies when the version gets rolled.
-      if (current_cpu == "arm64" || current_cpu == "x64") {
-        inputs += [ "//chrome/build/android-arm64.orderfile.txt" ]
-      } else if (current_cpu == "arm" || current_cpu == "x86") {
-        inputs += [ "//chrome/build/android-arm.orderfile.txt" ]
-      }
     }
   }
 }
--- a/DEPS
+++ b/DEPS
@@ -1198,7 +1198,7 @@ deps = {
       'packages': [
           {
               'package': 'chromium/chrome/android/orderfiles/arm',
-              'version_file': 'chrome/build/android-arm.orderfile.txt',
+              'version': '9U0UkrK-YxF8VGGRi5EEWPM86AJRIQQKE34Oz9j-8BUC',
           },
       ],
       'condition': 'checkout_android and non_git_source',
@@ -1209,7 +1209,7 @@ deps = {
       'packages': [
           {
               'package': 'chromium/chrome/android/orderfiles/arm64',
-              'version_file': 'chrome/build/android-arm64.orderfile.txt',
+              'version': '4mcLAUntdXQ1aI14KyVr3hjo3yll27o76edFmcwhXxsC',
           },
       ],
       'condition': 'checkout_android and non_git_source',
