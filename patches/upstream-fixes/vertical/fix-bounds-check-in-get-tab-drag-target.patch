--- a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
@@ -540,7 +540,11 @@
   if (!drag_handler_) {
     return nullptr;
   }
-  if (!tab_strip_view_->GetBoundsInScreen().Contains(point_in_screen)) {
+  // Use the region view's bounds rather than the tab strip view's bounds.
+  // The strip view's bounds shrink when tabs are removed (e.g., during
+  // detachment), which would prevent reattachment via system DnD because
+  // the cursor position is outside the now-smaller strip.
+  if (!GetBoundsInScreen().Contains(point_in_screen)) {
     return nullptr;
   }
 
