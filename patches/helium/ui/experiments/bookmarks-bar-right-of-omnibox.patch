--- a/chrome/browser/helium_flag_choices.h
+++ b/chrome/browser/helium_flag_choices.h
@@ -33,6 +33,8 @@ namespace helium {
   constexpr const char kHeliumCatUiCommandLine[] = "helium-cat-ui";
   constexpr const char kHeliumCatFixedAddressBarCommandLine[] =
       "helium-cat-fixed-address-bar";
+  constexpr const char kHeliumBookmarksBarRightOfOmniboxCommandLine[] =
+      "helium-bookmarks-bar-right-of-omnibox";
 
 }  // namespace helium
 
--- a/chrome/browser/helium_flag_entries.h
+++ b/chrome/browser/helium_flag_entries.h
@@ -36,4 +36,8 @@
      "[CAT] Fixed address bar width",
      "Prevents the address bar from shrinking in the CAT layout. Will push toolbar actions outside of bounds on small window sizes, beware. Works only when the main CAT flag is enabled. Early and buggy, stuff will break and/or look weird. Helium flag.",
      kOsDesktop, FEATURE_VALUE_TYPE(features::kHeliumCatFixedAddressBar)},
+    {helium::kHeliumBookmarksBarRightOfOmniboxCommandLine,
+     "Place bookmarks on the right of URL Bar",
+     "Moves the bookmarks bar into the toolbar, to the right of the URL bar. Not compatible with the CAT layout; the flag is ignored when CAT is enabled. Experimental; layout may break. Helium flag.",
+     kOsDesktop, FEATURE_VALUE_TYPE(features::kHeliumBookmarksBarRightOfOmnibox)},
 #endif  /* CHROME_BROWSER_HELIUM_FLAG_ENTRIES_H_ */
--- a/chrome/browser/ui/ui_features.cc
+++ b/chrome/browser/ui/ui_features.cc
@@ -25,6 +25,9 @@ BASE_FEATURE(kHeliumCatUi,
 BASE_FEATURE(kHeliumCatFixedAddressBar,
              base::FEATURE_DISABLED_BY_DEFAULT);
 
+BASE_FEATURE(kHeliumBookmarksBarRightOfOmnibox,
+             base::FEATURE_DISABLED_BY_DEFAULT);
+
 bool IsHeliumCatEnabled() {
   return base::FeatureList::IsEnabled(kHeliumCatUi);
 }
@@ -34,6 +37,11 @@ bool IsHeliumCatFixedAddressBar() {
       kHeliumCatFixedAddressBar);
 }
 
+bool IsHeliumBookmarksBarRightOfOmnibox() {
+  return base::FeatureList::IsEnabled(kHeliumBookmarksBarRightOfOmnibox) &&
+         !IsHeliumCatEnabled();
+}
+
 // Enables the tab dragging fallback when full window dragging is not supported
 // by the platform (e.g. Wayland). See https://crbug.com/896640
 BASE_FEATURE(kAllowWindowDragUsingSystemDragDrop,
--- a/chrome/browser/ui/ui_features.h
+++ b/chrome/browser/ui/ui_features.h
@@ -22,8 +22,10 @@ namespace features {
 
 BASE_DECLARE_FEATURE(kHeliumCatUi);
 BASE_DECLARE_FEATURE(kHeliumCatFixedAddressBar);
+BASE_DECLARE_FEATURE(kHeliumBookmarksBarRightOfOmnibox);
 bool IsHeliumCatEnabled();
 bool IsHeliumCatFixedAddressBar();
+bool IsHeliumBookmarksBarRightOfOmnibox();
 
 // TODO(crbug.com/40598679): Remove this when the tab dragging
 // interactive_ui_tests pass on Wayland.
--- a/chrome/browser/ui/views/bookmarks/bookmark_bar_view.cc
+++ b/chrome/browser/ui/views/bookmarks/bookmark_bar_view.cc
@@ -72,6 +72,7 @@
 #include "chrome/browser/ui/views/event_utils.h"
 #include "chrome/browser/ui/views/frame/browser_view.h"
 #include "chrome/browser/ui/views/frame/top_container_background.h"
+#include "chrome/browser/ui/views/frame/top_container_view.h"
 #include "chrome/browser/ui/views/side_panel/side_panel_ui.h"
 #include "chrome/browser/ui/views/side_panel/side_panel_util.h"
 #include "chrome/browser/ui/views/toolbar/toolbar_ink_drop_util.h"
@@ -584,6 +585,14 @@ int BookmarkBarView::GetLeadingMargin()
   static constexpr int kBookmarksBarLeadingMarginWithoutSavedTabGroups = 6;
   static constexpr int kBookmarksBarLeadingMarginWithSavedTabGroups = 6;
 
+  const bool inline_in_toolbar =
+      features::IsHeliumBookmarksBarRightOfOmnibox() && browser_view_ &&
+      parent() &&
+      parent() != static_cast<views::View*>(browser_view_->top_container());
+  if (inline_in_toolbar) {
+    return GetLayoutConstant(TOOLBAR_ELEMENT_PADDING);
+  }
+
   if (saved_tab_groups_separator_view_ &&
       saved_tab_groups_separator_view_->GetVisible()) {
     return kBookmarksBarLeadingMarginWithSavedTabGroups;
@@ -645,6 +654,79 @@ gfx::Size BookmarkBarView::CalculatePref
   int preferred_height = GetLayoutConstant(BOOKMARK_BAR_HEIGHT);
   prefsize.set_height(
       static_cast<int>(preferred_height * size_animation_.GetCurrentValue()));
+  if (features::IsHeliumBookmarksBarRightOfOmnibox()) {
+    const bool inline_in_toolbar =
+        features::IsHeliumBookmarksBarRightOfOmnibox() && browser_view_ &&
+        parent() &&
+        parent() != static_cast<views::View*>(browser_view_->top_container());
+    const int inline_padding = 1;
+    const int bookmark_bar_button_padding =
+        inline_in_toolbar ? inline_padding
+                          : GetLayoutConstant(BOOKMARK_BAR_BUTTON_PADDING);
+    const int trailing_margin =
+        inline_in_toolbar ? inline_padding
+                          : GetLayoutConstant(TOOLBAR_ELEMENT_PADDING);
+    int pref_width = GetLeadingMargin() + trailing_margin;
+
+    if (apps_page_shortcut_->GetVisible()) {
+      pref_width += apps_page_shortcut_->GetPreferredSize().width() +
+                    bookmark_bar_button_padding;
+    }
+    if (managed_bookmarks_button_->GetVisible()) {
+      pref_width += managed_bookmarks_button_->GetPreferredSize().width() +
+                    bookmark_bar_button_padding;
+    }
+    if (saved_tab_group_bar_ && saved_tab_group_bar_->GetVisible()) {
+      pref_width +=
+          saved_tab_group_bar_->CalculatePreferredWidthRestrictedBy(INT_MAX);
+      if (saved_tab_groups_separator_view_) {
+        pref_width += saved_tab_groups_separator_view_->GetPreferredSize()
+                          .width();
+      }
+    }
+    if (all_bookmarks_button_->GetVisible()) {
+      pref_width += all_bookmarks_button_->GetPreferredSize().width() +
+                    bookmark_bar_button_padding;
+    }
+    if (bookmarks_separator_view_->GetVisible()) {
+      pref_width += bookmarks_separator_view_->GetPreferredSize().width();
+    }
+
+    const BookmarkParentFolder bookmark_bar_folder =
+        BookmarkParentFolder::BookmarkBarFolder();
+    size_t bookmark_bar_children_count =
+        bookmark_service_ && bookmark_service_->loaded()
+            ? bookmark_service_->GetChildrenCount(bookmark_bar_folder)
+            : bookmark_buttons_.size();
+    int bookmark_buttons_width = 0;
+    if (bookmark_bar_children_count > 0) {
+      int known_width = 0;
+      for (const auto& bookmark_button_and_node : bookmark_buttons_) {
+        known_width += bookmark_button_and_node.first->GetPreferredSize().width();
+      }
+      const size_t known_count = bookmark_buttons_.size();
+      int average_width =
+          std::max(1, all_bookmarks_button_->GetPreferredSize().width());
+      if (known_count > 0) {
+        average_width =
+            std::max(average_width, known_width / static_cast<int>(known_count));
+      }
+
+      const size_t missing_count = bookmark_bar_children_count > known_count
+                                       ? (bookmark_bar_children_count -
+                                          known_count)
+                                       : 0;
+      bookmark_buttons_width = known_width +
+                               static_cast<int>(missing_count) * average_width;
+      if (bookmark_bar_children_count > 1) {
+        bookmark_buttons_width +=
+            static_cast<int>(bookmark_bar_children_count - 1) *
+            bookmark_bar_button_padding;
+      }
+    }
+    pref_width += bookmark_buttons_width;
+    prefsize.set_width(pref_width);
+  }
   return prefsize;
 }
 
@@ -699,9 +781,15 @@ void BookmarkBarView::Layout(PassKey) {
     return;
   }
 
+  const bool inline_in_toolbar =
+      features::IsHeliumBookmarksBarRightOfOmnibox() && browser_view_ &&
+      parent() &&
+      parent() != static_cast<views::View*>(browser_view_->top_container());
+
   int x = GetLeadingMargin();
-  static constexpr int kBookmarkBarTrailingMargin = 6;
-  int width = View::width() - x - kBookmarkBarTrailingMargin;
+  const int inline_padding = 1;
+  const int trailing_margin = inline_in_toolbar ? inline_padding : 6;
+  int width = View::width() - x - trailing_margin;
 
   const int button_height = GetLayoutConstant(BOOKMARK_BAR_BUTTON_HEIGHT);
 
@@ -710,7 +798,7 @@ void BookmarkBarView::Layout(PassKey) {
   // bottom margin of the toolbar into the button position.
   int toolbar_bottom_margin = 0;
   // Note: |browser_view_| may be null during tests.
-  if (browser_view_ && !browser_view_->IsFullscreen()) {
+  if (browser_view_ && !browser_view_->IsFullscreen() && !inline_in_toolbar) {
     toolbar_bottom_margin =
         browser_view_->toolbar()->height() -
         browser_view_->GetLocationBarView()->bounds().bottom();
@@ -730,21 +818,34 @@ void BookmarkBarView::Layout(PassKey) {
                                      ? all_bookmarks_button_->GetPreferredSize()
                                      : gfx::Size();
   gfx::Size overflow_pref = overflow_button_->GetPreferredSize();
+  if (inline_in_toolbar) {
+    bookmarks_separator_view_->UpdateBorderAndPreferredSize(
+        gfx::Insets::VH(0, inline_padding));
+  } else {
+    bookmarks_separator_view_->UpdateBorderAndPreferredSize(
+        gfx::Insets::VH(0, 8));
+  }
   gfx::Size bookmarks_separator_pref =
       bookmarks_separator_view_->GetPreferredSize();
+  if (!bookmarks_separator_view_->GetVisible()) {
+    bookmarks_separator_pref = gfx::Size();
+  }
   gfx::Size apps_page_shortcut_pref =
       apps_page_shortcut_->GetVisible()
           ? apps_page_shortcut_->GetPreferredSize()
           : gfx::Size();
 
   const int bookmark_bar_button_padding =
-      GetLayoutConstant(BOOKMARK_BAR_BUTTON_PADDING);
+      inline_in_toolbar ? inline_padding
+                        : GetLayoutConstant(BOOKMARK_BAR_BUTTON_PADDING);
 
-  int max_x = GetLeadingMargin() + width - overflow_pref.width() -
-              bookmarks_separator_pref.width();
+  int max_x_no_overflow =
+      GetLeadingMargin() + width - bookmarks_separator_pref.width();
   if (all_bookmarks_button_->GetVisible()) {
-    max_x -= all_bookmarks_pref.width() + bookmark_bar_button_padding;
+    max_x_no_overflow -=
+        all_bookmarks_pref.width() + bookmark_bar_button_padding;
   }
+  int max_x = max_x_no_overflow - overflow_pref.width();
 
   // Start with the apps page shortcut button.
   if (apps_page_shortcut_->GetVisible()) {
@@ -769,6 +870,16 @@ void BookmarkBarView::Layout(PassKey) {
           ? bookmark_service_->GetChildrenCount(bookmark_bar_folder)
           : 0u;
 
+  if (inline_in_toolbar && bookmark_bar_children_count) {
+    BookmarkParentFolderChildren bookmark_bar_children =
+        bookmark_service_->GetChildren(bookmark_bar_folder);
+    for (size_t i = bookmark_buttons_.size(); i < bookmark_bar_children_count;
+         ++i) {
+      InsertBookmarkButtonAtIndex(
+          CreateBookmarkButton(bookmark_bar_children[i], i), i);
+    }
+  }
+
   int saved_tab_group_bar_width = 0;
   if (saved_tab_group_bar_ && saved_tab_group_bar_->GetVisible()) {
     // Manually set the overflow(Everything) button's preferred width to be
@@ -841,11 +952,32 @@ void BookmarkBarView::Layout(PassKey) {
     }
   }
 
+  bool can_fit_all_bookmarks = false;
+  if (inline_in_toolbar && bookmark_bar_children_count) {
+    int total_bookmarks_width = 0;
+    for (const auto& bookmark_button_and_node : bookmark_buttons_) {
+      total_bookmarks_width +=
+          bookmark_button_and_node.first->GetPreferredSize().width();
+    }
+    if (bookmark_buttons_.size() > 1) {
+      total_bookmarks_width +=
+          (bookmark_buttons_.size() - 1) * bookmark_bar_button_padding;
+    }
+    const int available_no_overflow = max_x_no_overflow - x;
+    can_fit_all_bookmarks =
+        available_no_overflow >= 0 &&
+        total_bookmarks_width <= available_no_overflow;
+    if (can_fit_all_bookmarks) {
+      max_x = max_x_no_overflow;
+    }
+  }
+
+  int last_visible_right = x;
   if (bookmark_bar_children_count) {
-    bool can_render_button_bounds = x < max_x;
-    size_t button_count = bookmark_buttons_.size();
     BookmarkParentFolderChildren bookmark_bar_children =
         bookmark_service_->GetChildren(bookmark_bar_folder);
+    bool can_render_button_bounds = x <= max_x;
+    size_t button_count = bookmark_buttons_.size();
     for (size_t i = 0; i <= button_count; ++i) {
       if (i == button_count) {
         // Add another button if there is room for it (and there is another
@@ -863,22 +995,31 @@ void BookmarkBarView::Layout(PassKey) {
       // If the child view can fit in the bookmarks comfortably, make it visible
       // and set its bounds.
       gfx::Size pref = child->GetPreferredSize();
-      int next_x = x + pref.width() + bookmark_bar_button_padding;
-      can_render_button_bounds = next_x < max_x;
+      int next_x = x + pref.width();
+      int next_x_with_padding = next_x;
+      if (i + 1 < button_count) {
+        next_x_with_padding += bookmark_bar_button_padding;
+      }
+      can_render_button_bounds = next_x_with_padding <= max_x;
       child->SetVisible(can_render_button_bounds);
       // Only need to set bounds if the view is actually visible.
       if (can_render_button_bounds) {
         child->SetBounds(x, y, pref.width(), button_height);
+        last_visible_right = next_x;
       }
-      x = next_x;
+      x = next_x_with_padding;
     }
   }
 
-  const bool show_bookmarks_overflow =
+  bool show_bookmarks_overflow =
       bookmark_service_->loaded() &&
       (bookmark_bar_children_count > bookmark_buttons_.size() ||
        (!bookmark_buttons_.empty() &&
         !bookmark_buttons_.back().first->GetVisible()));
+  if (inline_in_toolbar && bookmark_service_->loaded()) {
+    show_bookmarks_overflow =
+        bookmark_bar_children_count > 0 && !can_fit_all_bookmarks;
+  }
 
   // Set the visibility of the tab group separator if there are groups and
   // bookmarks.
@@ -892,12 +1033,24 @@ void BookmarkBarView::Layout(PassKey) {
   }
 
   // Layout the right side buttons.
-  x = max_x + bookmark_bar_button_padding;
+  if (inline_in_toolbar) {
+    x = last_visible_right + bookmark_bar_button_padding;
+  } else {
+    x = max_x + bookmark_bar_button_padding;
+  }
 
   // The overflow button.
-  overflow_button_->SetBounds(x, y, overflow_pref.width(), button_height);
-  overflow_button_->SetVisible(show_bookmarks_overflow);
-  x += overflow_pref.width();
+  if (inline_in_toolbar) {
+    overflow_button_->SetVisible(show_bookmarks_overflow);
+    if (show_bookmarks_overflow) {
+      overflow_button_->SetBounds(x, y, overflow_pref.width(), button_height);
+      x += overflow_pref.width();
+    }
+  } else {
+    overflow_button_->SetBounds(x, y, overflow_pref.width(), button_height);
+    overflow_button_->SetVisible(show_bookmarks_overflow);
+    x += overflow_pref.width();
+  }
 
   // Bookmarks Separator.
   if (bookmarks_separator_view_->GetVisible()) {
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -5571,14 +5571,30 @@ bool BrowserView::MaybeShowBookmarkBar(W
   // BrowserViewLayout is responsible for handling the final visibility and
   // animation of the BookmarkBar.
   bool needs_layout = false;
-  if (show_bookmark_bar && !bookmark_bar_view_->parent()) {
-    // Add the bookmark bar to the view hierarchy if it might be shown.
-    top_container_->AddChildView(bookmark_bar_view_.get());
-    needs_layout = true;
-  } else if (!show_bookmark_bar && bookmark_bar_view_->parent()) {
-    // Remove the bookmark bar from the view hierarchy if it should be hidden.
-    top_container_->RemoveChildView(bookmark_bar_view_.get());
-    needs_layout = true;
+  if (features::IsHeliumBookmarksBarRightOfOmnibox()) {
+    const bool infobar_visible =
+        infobar_container_ && !infobar_container_->IsEmpty() &&
+        (!GetWidget()->IsFullscreen() ||
+         !infobar_container_->ShouldHideInFullscreen());
+    bookmark_bar_view_->SetInfoBarVisible(infobar_visible);
+
+    if (show_bookmark_bar) {
+      toolbar_->SetBookmarkBarView(bookmark_bar_view_.get());
+      needs_layout = true;
+    } else if (bookmark_bar_view_->parent()) {
+      toolbar_->SetBookmarkBarView(nullptr);
+      needs_layout = true;
+    }
+  } else {
+    if (show_bookmark_bar && !bookmark_bar_view_->parent()) {
+      // Add the bookmark bar to the view hierarchy if it might be shown.
+      top_container_->AddChildView(bookmark_bar_view_.get());
+      needs_layout = true;
+    } else if (!show_bookmark_bar && bookmark_bar_view_->parent()) {
+      // Remove the bookmark bar from the view hierarchy if it should be hidden.
+      top_container_->RemoveChildView(bookmark_bar_view_.get());
+      needs_layout = true;
+    }
   }
 
   // Check for updates to the desired size.
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_impl_old.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_impl_old.cc
@@ -12,6 +12,7 @@
 #include "build/build_config.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/layout_constants.h"
+#include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/tabs/features.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
 #include "chrome/browser/ui/views/bookmarks/bookmark_bar_view.h"
@@ -553,6 +554,14 @@ void BrowserViewLayoutImplOld::LayoutBoo
 }
 
 void BrowserViewLayoutImplOld::LayoutBookmarkBar(gfx::Rect& available_bounds) {
+  if (features::IsHeliumBookmarksBarRightOfOmnibox() && views().bookmark_bar &&
+      views().bookmark_bar->parent() != views().top_container) {
+    views().bookmark_bar->SetInfoBarVisible(IsInfobarVisible());
+    // Avoid routing visibility through the top container's layout manager.
+    views().bookmark_bar->SetVisible(delegate().IsBookmarkBarVisible());
+    return;
+  }
+
   if (!delegate().IsBookmarkBarVisible()) {
     SetViewVisibility(views().bookmark_bar, false);
     // TODO(jamescook): Don't change the bookmark bar height when it is
--- a/chrome/browser/ui/views/toolbar/toolbar_view.h
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.h
@@ -44,6 +44,7 @@
 class AppMenuButton;
 class AvatarToolbarButton;
 class BatterySaverButton;
+class BookmarkBarView;
 class BrowserAppMenuButton;
 class Browser;
 class ExtensionsToolbarButton;
@@ -146,6 +147,8 @@ class ToolbarView : public views::Access
 
   // Shows a bookmark bubble and anchors it appropriately.
   void ShowBookmarkBubble(const GURL& url, bool already_bookmarked);
+  // Moves the bookmark bar into the toolbar (right of the address bar).
+  void SetBookmarkBarView(BookmarkBarView* bookmark_bar_view);
 
   // Returns the tab strip region view if CAT feature is enabled, nullptr otherwise.
   views::View* GetTabStripRegionView() const { return tab_strip_region_view_.get(); }
@@ -237,6 +240,8 @@ class ToolbarView : public views::Access
   // Declarative layout for child controls.
   void InitLayout();
 
+  void UpdateBookmarkBarLayout();
+
   // Logic that must be done on initialization and then on layout.
   void LayoutCommon();
 
@@ -316,6 +321,7 @@ class ToolbarView : public views::Access
   raw_ptr<CustomTabBarView> custom_tab_bar_ = nullptr;
   raw_ptr<LocationBarView> location_bar_ = nullptr;
   raw_ptr<ExtensionsToolbarContainer> extensions_container_ = nullptr;
+  raw_ptr<views::View> bookmark_bar_divider_ = nullptr;
   raw_ptr<views::View> toolbar_divider_ = nullptr;
   raw_ptr<BatterySaverButton> battery_saver_button_ = nullptr;
   raw_ptr<PerformanceInterventionButton> performance_intervention_button_ =
@@ -327,6 +333,7 @@ class ToolbarView : public views::Access
   raw_ptr<BrowserAppMenuButton> app_menu_button_ = nullptr;
   raw_ptr<views::View> new_tab_button_ = nullptr;
   raw_ptr<PinnedActionToolbarButton> tab_search_button_ = nullptr;
+  raw_ptr<BookmarkBarView> bookmark_bar_view_ = nullptr;
   raw_ptr<views::View> tab_strip_region_view_ = nullptr;
 
   const raw_ptr<Browser> browser_;
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -50,6 +50,7 @@
 #include "chrome/browser/ui/toolbar/pinned_toolbar/tab_search_toolbar_button_controller.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/view_ids.h"
+#include "chrome/browser/ui/views/bookmarks/bookmark_bar_view.h"
 #include "chrome/browser/ui/views/bookmarks/bookmark_bubble_view.h"
 #include "chrome/browser/ui/views/extensions/extension_popup.h"
 #include "chrome/browser/ui/views/extensions/extensions_toolbar_button.h"
@@ -150,6 +151,11 @@ DEFINE_UI_CLASS_PROPERTY_KEY(bool, kActi
 namespace {
 
 constexpr int kPreferredCompactWidth = 350;
+constexpr int kToolbarFlexOrderOffset = 1000;
+constexpr int kLocationBarFlexOrder = kToolbarFlexOrderOffset + 1;
+constexpr int kToolbarActionsFlexOrder = kToolbarFlexOrderOffset + 2;
+constexpr int kExtensionsFlexOrder = kToolbarFlexOrderOffset + 3;
+constexpr int kBookmarkBarFlexOrder = kToolbarFlexOrderOffset + 4;
 
 // Gets the display mode for a given browser.
 ToolbarView::DisplayMode GetDisplayMode(Browser* browser) {
@@ -348,6 +354,7 @@ void ToolbarView::Init() {
 
   std::unique_ptr<ExtensionsToolbarContainer> extensions_container;
   std::unique_ptr<views::View> toolbar_divider;
+  std::unique_ptr<views::View> bookmark_bar_divider;
 
   // Do not create the extensions or browser actions container if it is a guest
   // profile (only regular and incognito profiles host extensions).
@@ -356,6 +363,7 @@ void ToolbarView::Init() {
         std::make_unique<ExtensionsToolbarContainer>(browser_);
 
     toolbar_divider = std::make_unique<views::View>();
+    bookmark_bar_divider = std::make_unique<views::View>();
   }
 
   std::unique_ptr<MediaToolbarButtonView> media_button;
@@ -386,6 +394,17 @@ void ToolbarView::Init() {
 
   location_bar_ = container_view_->AddChildView(std::move(location_bar));
 
+  if (bookmark_bar_divider) {
+    bookmark_bar_divider_ =
+        container_view_->AddChildView(std::move(bookmark_bar_divider));
+    bookmark_bar_divider_->SetPreferredSize(
+        gfx::Size(GetLayoutConstant(TOOLBAR_DIVIDER_WIDTH),
+                  GetLayoutConstant(TOOLBAR_DIVIDER_HEIGHT)));
+    bookmark_bar_divider_->SetBackground(views::CreateRoundedRectBackground(
+        kColorTabDividerFrameActive,
+        GetLayoutConstant(TOOLBAR_DIVIDER_CORNER_RADIUS)));
+  }
+
   if (extensions_container) {
     extensions_container_ =
         container_view_->AddChildView(std::move(extensions_container));
@@ -823,6 +842,53 @@ void ToolbarView::ShowBookmarkBubble(con
                                  already_bookmarked);
 }
 
+void ToolbarView::SetBookmarkBarView(BookmarkBarView* bookmark_bar_view) {
+  if (display_mode_ != DisplayMode::NORMAL || !container_view_) {
+    return;
+  }
+
+  if (bookmark_bar_view_ && bookmark_bar_view_ != bookmark_bar_view &&
+      bookmark_bar_view_->parent() == container_view_) {
+    container_view_->RemoveChildView(bookmark_bar_view_);
+  }
+
+  bookmark_bar_view_ = bookmark_bar_view;
+  if (!bookmark_bar_view_) {
+    return;
+  }
+
+  if (bookmark_bar_view_->parent() &&
+      bookmark_bar_view_->parent() != container_view_) {
+    bookmark_bar_view_->parent()->RemoveChildView(bookmark_bar_view_);
+  }
+
+  std::optional<size_t> insertion_index =
+      location_bar_ ? container_view_->GetIndexOf(location_bar_) : std::nullopt;
+  if (tab_strip_region_view_ &&
+      tab_strip_region_view_->parent() == container_view_) {
+    auto tab_strip_index = container_view_->GetIndexOf(tab_strip_region_view_);
+    if (tab_strip_index &&
+        (!insertion_index || *tab_strip_index > *insertion_index)) {
+      insertion_index = *tab_strip_index;
+    }
+  }
+
+  if (bookmark_bar_view_->parent() != container_view_) {
+    if (insertion_index) {
+      container_view_->AddChildViewAt(bookmark_bar_view_.get(),
+                                      *insertion_index + 1);
+    } else {
+      container_view_->AddChildView(bookmark_bar_view_.get());
+    }
+  } else if (insertion_index) {
+    container_view_->ReorderChildView(bookmark_bar_view_.get(),
+                                      *insertion_index + 1);
+  }
+
+  UpdateBookmarkBarLayout();
+  InvalidateLayout();
+}
+
 views::Button* ToolbarView::GetChromeLabsButton() const {
   return browser_->GetFeatures()
       .chrome_labs_coordinator()
@@ -1030,16 +1096,16 @@ views::View* ToolbarView::GetDefaultFocu
 
 void ToolbarView::InitLayout() {
   const int default_margin = GetLayoutConstant(TOOLBAR_ICON_DEFAULT_MARGIN);
+  const bool inline_bookmarks = features::IsHeliumBookmarksBarRightOfOmnibox();
+  const int location_bar_margin =
+      inline_bookmarks ? GetLayoutConstant(TOOLBAR_ELEMENT_PADDING)
+                       : GetLayoutConstant(TOOLBAR_STANDARD_SPACING);
   // TODO(dfried): rename this constant.
-  const int location_bar_margin = GetLayoutConstant(TOOLBAR_STANDARD_SPACING);
 
-  // Shift previously flex-able elements' order by `kOrderOffset`.
+  // Shift previously flex-able elements' order by `kToolbarFlexOrderOffset`.
   // This will cause them to be the first ones to drop out or shrink to minimum.
-  // Order 1 - kOrderOffset will be assigned to new flex-able elements.
-  constexpr int kOrderOffset = 1000;
-  constexpr int kLocationBarFlexOrder = kOrderOffset + 1;
-  constexpr int kToolbarActionsFlexOrder = kOrderOffset + 2;
-  constexpr int kExtensionsFlexOrder = kOrderOffset + 3;
+  // Order 1 - kToolbarFlexOrderOffset will be assigned to new flex-able
+  // elements.
 
   auto min_location_bar_flex = features::IsHeliumCatFixedAddressBar() ?
                                   views::MinimumFlexSizeRule::kPreferred :
@@ -1068,6 +1134,8 @@ void ToolbarView::InitLayout() {
                                gfx::Insets::TLBR(0, location_bar_margin, 0, 0));
   }
 
+  UpdateBookmarkBarLayout();
+
   if (extensions_container_) {
     const views::FlexSpecification extensions_flex_rule =
         views::FlexSpecification(
@@ -1094,6 +1162,15 @@ void ToolbarView::InitLayout() {
         gfx::Insets::VH(0, GetLayoutConstant(TOOLBAR_DIVIDER_SPACING)));
   }
 
+  if (bookmark_bar_divider_) {
+    const int divider_margin =
+        features::IsHeliumBookmarksBarRightOfOmnibox()
+            ? 1
+            : GetLayoutConstant(TOOLBAR_ELEMENT_PADDING);
+    bookmark_bar_divider_->SetProperty(
+        views::kMarginsKey, gfx::Insets::VH(0, divider_margin));
+  }
+
   constexpr int kToolbarFlexOrderStart = 1;
 
   // TODO(crbug.com/40929989): Ignore containers till issue addressed.
@@ -1157,6 +1234,15 @@ void ToolbarView::LayoutCommon() {
     views::ManualLayoutUtil(layout_manager_)
         .SetViewHidden(toolbar_divider_, !extensions_container_->GetVisible());
   }
+
+  if (bookmark_bar_divider_) {
+    const bool show_divider =
+        features::IsHeliumBookmarksBarRightOfOmnibox() && bookmark_bar_view_ &&
+        bookmark_bar_view_->GetVisible() && extensions_container_ &&
+        extensions_container_->GetVisible();
+    views::ManualLayoutUtil(layout_manager_)
+        .SetViewHidden(bookmark_bar_divider_, !show_divider);
+  }
   // Cast button visibility is controlled externally.
 }
 
@@ -1369,18 +1455,51 @@ void ToolbarView::OnTouchUiChanged() {
     // Update the internal margins for touch layout.
     // TODO(dfried): I think we can do better than this by making the touch UI
     // code cleaner.
+    const bool inline_bookmarks =
+        features::IsHeliumBookmarksBarRightOfOmnibox();
     const int default_margin = GetLayoutConstant(TOOLBAR_ELEMENT_PADDING);
-    const int location_bar_margin = GetLayoutConstant(TOOLBAR_STANDARD_SPACING);
+    const int location_bar_margin =
+        inline_bookmarks ? GetLayoutConstant(TOOLBAR_ELEMENT_PADDING)
+                         : GetLayoutConstant(TOOLBAR_STANDARD_SPACING);
     layout_manager_->SetDefault(views::kMarginsKey,
                                 gfx::Insets::VH(0, default_margin));
     location_bar_->SetProperty(views::kMarginsKey,
                                gfx::Insets::VH(0, location_bar_margin));
+    UpdateBookmarkBarLayout();
 
     LoadImages();
     PreferredSizeChanged();
   }
 }
 
+void ToolbarView::UpdateBookmarkBarLayout() {
+  if (!bookmark_bar_view_) {
+    return;
+  }
+
+  const bool inline_bookmarks =
+      features::IsHeliumBookmarksBarRightOfOmnibox();
+  const int bookmark_bar_margin =
+      inline_bookmarks ? 0 : GetLayoutConstant(TOOLBAR_STANDARD_SPACING);
+  const int bookmark_bar_flex_order =
+      inline_bookmarks ? kLocationBarFlexOrder - 1 : kBookmarkBarFlexOrder;
+  const auto bookmark_bar_max_rule =
+      inline_bookmarks ? views::MaximumFlexSizeRule::kPreferred
+                       : views::MaximumFlexSizeRule::kUnbounded;
+  views::FlexSpecification bookmark_bar_flex_rule =
+      views::FlexSpecification(views::MinimumFlexSizeRule::kScaleToMinimum,
+                               bookmark_bar_max_rule)
+          .WithOrder(bookmark_bar_flex_order);
+  if (inline_bookmarks) {
+    bookmark_bar_flex_rule = bookmark_bar_flex_rule.WithWeight(0);
+  }
+
+  bookmark_bar_view_->SetProperty(views::kFlexBehaviorKey,
+                                  bookmark_bar_flex_rule);
+  bookmark_bar_view_->SetProperty(views::kMarginsKey,
+                                  gfx::Insets::VH(0, bookmark_bar_margin));
+}
+
 void ToolbarView::OnTabStripModelChanged(
     TabStripModel* tab_strip_model,
     const TabStripModelChange& change,
