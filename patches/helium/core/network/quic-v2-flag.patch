--- a/components/network_session_configurator/common/network_switch_list.h
+++ b/components/network_session_configurator/common/network_switch_list.h
@@ -56,6 +56,9 @@ NETWORK_SWITCH(kQuicMaxPacketLength, "qu
 // Specifies the version of QUIC to use.
 NETWORK_SWITCH(kQuicVersion, "quic-version")
 
+// Restricts eligible QUIC connections to QUIC v2.
+NETWORK_SWITCH(kHeliumQuicV2, "quic-v2-only")
+
 // Allows for forcing socket connections to http/https to use fixed ports.
 NETWORK_SWITCH(kTestingFixedHttpPort, "testing-fixed-http-port")
 NETWORK_SWITCH(kTestingFixedHttpsPort, "testing-fixed-https-port")
--- a/chrome/browser/helium_flag_entries.h
+++ b/chrome/browser/helium_flag_entries.h
@@ -35,4 +35,8 @@
      "Disable Encrypted Client Hello (ECH)",
      "Disables TLS Encrypted Client Hello, reducing traffic privacy when accessing websites that support ECH. Use this flag only if ECH is blocked on your network. Helium flag.",
      kOsAll, SINGLE_VALUE_TYPE(helium::kDisableEchCommandLine)},
+    {switches::kHeliumQuicV2,
+     "Restrict HTTP/3 to QUIC v2",
+     "Restricts HTTP/3 connections to QUIC v2 instead of default QUIC v1. This may help on networks that block QUIC v1 but do not recognize QUIC v2. It does not force QUIC on unsupported sites. Connections may fall back to TCP when the server does not support QUIC v2. Helium flag.",
+     kOsAll, SINGLE_VALUE_TYPE(switches::kHeliumQuicV2)},
 #endif  /* CHROME_BROWSER_HELIUM_FLAG_ENTRIES_H_ */
--- a/components/network_session_configurator/browser/network_session_configurator.cc
+++ b/components/network_session_configurator/browser/network_session_configurator.cc
@@ -807,6 +807,12 @@ void ParseCommandLineAndFieldTrials(cons
         quic_params->supported_versions = supported_versions;
     }
 
+    if (command_line.HasSwitch(switches::kHeliumQuicV2)) {
+      const quic::ParsedQuicVersion version = quic::ParsedQuicVersion::RFCv2();
+      quic::QuicEnableVersion(version);
+      quic_params->supported_versions = {version};
+    }
+
     if (command_line.HasSwitch(switches::kOriginToForceQuicOn)) {
       std::string origins =
           command_line.GetSwitchValueASCII(switches::kOriginToForceQuicOn);
